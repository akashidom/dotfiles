return {
    -- Blur
    blurEnabled                = true,
    blurSpecialWs              = false,
    blurPopups                 = false,
    blurInputMethods           = false,
    blurXray                   = false,
}
